# AGENTS.md

## 项目一句话

{{project.description}}

## 全局约束

- 回答使用{{constraints.docLanguage}}
{{#if (eq constraints.docLanguage "中文")}}
- 中英文间加空格
{{/if}}
- 讨论设计或问题时优先输出伪代码
{{#each constraints.forbiddenCommands}}
- 禁止执行 `{{this}}`，必须找用户确认
{{/each}}
- 禁止修改 `README.md` 和 `docs/architecture.md`，除非任务明确要求

## 命令

- 安装依赖：`{{commands.install}}`
- 本地启动：`{{commands.dev}}`
{{#if commands.build}}
- 构建：`{{commands.build}}`
{{/if}}
- 测试：`{{commands.test}}`
- 代码检查：`{{commands.lint}}`
{{#if commands.format}}
- 代码格式化：`{{commands.format}}`
{{/if}}

## 质量门禁

{{#if commands.format}}
- 代码改动在交付前默认执行：`{{commands.lint}} && {{commands.format}} && {{commands.test}}`
{{else}}
- 代码改动在交付前默认执行：`{{commands.lint}} && {{commands.test}}`
{{/if}}
- 纯文档或纯规划任务可不执行全量测试

## 执行门禁

以下规则，请严格遵守：

1. 接到新任务时（MUST）：先读取 `docs/agents/tasks.md`，理解完整工作流。
2. 开始编码前（MUST）：根据规模在 Git Worktree 或 jj new 中开辟独立工作区，参考 `docs/agents/vcs.md`。
3. 操作版本控制前（MUST）：先读取 `docs/agents/vcs.md`，遵循项目的版本控制规范。

## 按需阅读

| 场景 | 文档 | 内容 |
| --- | --- | --- |
| 探索方案 / 脑暴 | `docs/agents/brainstorm.md` | 方案生成、对比、反馈模板 |
| 方案太保守 / 卡住了 | `docs/agents/think-out-of-box.md` | 质疑前提、挑战方案 |
| 深入设计 | `docs/agents/design.md` | 决策流程、方案文档模板 |
| 做技术决策 / 写代码 | `docs/agents/principles.md` | 核心原则、底线 |
| 编写测试 | `docs/agents/testing.md` | 测试策略、Red/Green/Refactor |
| 了解系统全景 | `docs/architecture.md` | 系统架构、数据模型、关键决策 |
| 提交前自检 / 做评审 | `docs/agents/review.md` | 自查清单、评审规范、反馈格式 |
| ADR 写作指南 | `docs/agents/adr.md` | ADR 模板、分类、记录位置 |
| 记录技术债务 | `docs/tech_debt_tracker.md` | WAIVER 记录与追踪 |
