# 代码评审

你在提交代码前自检的规范，以及如何应对评审反馈。人类阅读此文档可以了解 agent 遵循的评审准则。

## 触发时机

准备进入「交付（需人工确认）」前，必须执行以下自检。普通文档任务可跳过结构性反模式自检，但 `AGENTS.md`、`docs/agents/*` 和 agent 指南模板变更必须执行 Agent 指令质量检查。

## 自检：结构性反模式

每次自检必须逐项扫描以下三类深层问题。linter 查不出这些，reviewer 也容易漏——一旦合入就是慢性病。

### 补丁式修复

**问题**：靠加条件分支、特殊处理、兜底逻辑压住表象，而没有回到根因或收敛数据流/状态机。

**检查方法**：
- 这次改动有没有增加新的分支来处理"特殊情况"，而不是修正上游？
- 有没有在调用链末端加保护，而不是在数据源头收敛？
- 有没有用重试/忽略掩盖不稳定的外部依赖，而不是加超时和熔断？

### 内部过度防御

**问题**：在内部调用链上增加多余的校验、重复检查、静默降级，或者信任边界内层层包裹转换。

**检查方法**：
- 同一个条件是否在多层中被重复检查？
- 是否校验了函数签名已经保证的状态？（如返回值已保证非空，又判空）
- 是否有吞掉错误、返回兜底值、静默跳过？

### 热路径退化

**问题**：在频繁执行的路径上引入了不必要的对象创建、数据转换、同步等待或 I/O。

**检查方法**：
- 循环或高频回调中是否有对象分配或临时数据结构的构建？
- 日志语句是否在高频路径上执行，且格式化开销大于实际逻辑？
- 是否存在不必要的数据复制或编解码（如字符串与字节互转、序列化/反序列化）？

### Agent 指令质量（仅文档 / 指南变更）

**问题**：agent 指南中的规则互相冲突、触发条件不清，或者要求 agent 执行未授权的副作用。

**检查方法**：
- 是否存在两个文档对同一行为给出不同要求？
- 是否把写文件、pull/rebase、迁移、发布等副作用设为默认动作，而没有要求确认？
- 产物目录、索引位置和引用路径是否明确，首次创建时该做什么是否说清楚？
- tiny/fix 等明确任务是否有直接执行路径，避免被流程强制拖入脑暴或设计？

## 输出：四维结论表

交付说明中必须贴出以下表格，每项逐一判定：

| 维度 | 含义 | 判定 |
| --- | --- | --- |
| **Scope Control** | 改动范围是否最小化，没有带进无关变更 | PASS / BLOCK / WAIVER / N/A |
| **Design Fit** | 方案是否与现有架构、状态机、数据流一致 | PASS / BLOCK / WAIVER / N/A |
| **Invariant Clarity** | 不变量是否显式，没有隐式假设和过度防御 | PASS / BLOCK / WAIVER / N/A |
| **Hot Path Perf** | 关键路径是否引入了性能退化 | PASS / BLOCK / WAIVER / N/A |

判定规则：
- **PASS**：自查通过，无需讨论
- **BLOCK**：存在必须修复的问题，修复后才能交付
- **WAIVER**：已知问题但明确接受——必须同步记录到 `docs/tech_debt_tracker.md`
- **N/A**：本次改动不涉及该维度

有 BLOCK 时先修复再交付。WAIVER 仅允许用于明确讨论过的 workaround，不允许自己给自己豁免。

## 交付说明格式

```
## 自检报告

### Findings
- `src/xxx.ts:42` — 此处存在补丁式修复，根因在...
- （无新增问题则写"无"）

### 结论

| 维度 | 判定 |
| --- | --- |
| Scope Control | PASS |
| Design Fit | PASS |
| Invariant Clarity | PASS |
| Hot Path Perf | N/A |

### 备注
普通文档任务，跳过结构性反模式自检。（仅在不修改 agent 指南时使用）
```

## 评审中你会被查什么

除自检覆盖的三类反模式外，reviewer 还会关注：

{{#if (eq review.strictness "严格")}}
- ⭐ **代码结构**：模块划分合理，职责清晰
- ⭐ **可读性**：无需上下文解释就能理解意图
- ⭐ **可维护性**：未来改动不会牵一发动全身
- ⭐ **测试覆盖**：核心路径和边界条件有测试保护
- ⭐ **一致性**：和项目中已有代码的风格一致
- ⭐ **无重复**：优先复用而非重写
{{else if (eq review.strictness "适中")}}
- ⭐ **代码结构**：模块划分合理，职责清晰
- **可读性**：无需上下文解释就能理解意图
- **可维护性**：未来改动不会牵一发动全身
- ⭐ **测试覆盖**：核心路径和边界条件有测试保护
- **一致性**：和项目中已有代码的风格一致
- **无重复**：优先复用而非重写
{{else}}
- ⭐ **代码结构**：模块划分合理，职责清晰
- **可读性**：无需上下文解释就能理解意图
- **可维护性**：未来改动不会牵一发动全身
- **测试覆盖**：核心路径和边界条件有测试保护
- **一致性**：和项目中已有代码的风格一致
- **无重复**：优先复用而非重写
{{/if}}

## 收到反馈后怎么回应

- **逐条回复**：对每一条反馈给出回应——已修改 / 不修改，说明理由
- **不防御**：review 针对的是代码，不是你。不需要为每个决定辩护，只需要解释
- **追问不清楚的**：如果反馈描述模糊，请求具体化——"你指的文件名+行号是什么？"
- **提取模式**：如果同一类反馈反复出现，记下来，以后主动避免
