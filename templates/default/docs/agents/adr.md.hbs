# 架构决策记录 (ADR)

本项目使用 ADR（Architecture Decision Records）记录关键技术决策。每个影响软件项目或产品的重大架构决策，都应创建一条 ADR。

## ADR 模板

每条 ADR 按以下格式记录在 `docs/adrs/` 目录下，文件名格式：`YYYY-MM-DD-<简述>.md`。

```markdown
# ADR-NNN：决策标题

- **日期**：YYYY-MM-DD
- **状态**：提议 / 已采纳 / 已废弃 / 被替代
- **替代**：<替代了哪条 ADR（如有）>

## 背景

<描述当前面临的问题、约束和驱动力。说明为什么需要做这个决策。>

## 决策

<描述做出的决策，以及选择这个方案的理由。>

## 方案对比

| 方案 | 优点 | 缺点 | 风险 |
| --- | --- | --- | --- |
| A | ... | ... | ... |
| B | ... | ... | ... |

## 后果

<说明这个决策带来的影响：什么变得更容易，什么变得更困难。>

### 正面影响
- ...

### 负面影响 / 需要付出的代价
- ...

## 相关

- <关联的 ADR、文档或 Issue>
```

## 需要记录 ADR 的场景

以下任一类别的决策都应该创建 ADR：

### 1. 结构

系统架构层面的决策，影响整体组织方式：

- **架构模式**：微服务、单体、事件驱动、CQRS 等
- **模块划分**：如何将系统拆分为组件/模块
- **部署拓扑**：服务如何分布、如何通信
- **多租户策略**：数据隔离、资源隔离方案

示例：
```
- ADR-001：选择微服务架构而非单体
- ADR-002：订单模块与支付模块的边界定义
- ADR-003：前后端通信采用 BFF 模式
```

### 2. 非功能需求

可靠性、安全性、性能等质量属性的保障方案：

- **安全性**：认证方案、授权模型、数据加密策略
- **高可用性**：容错设计、降级策略、熔断机制
- **容灾**：备份策略、多活方案、灾难恢复
- **性能**：缓存策略、数据分片、异步处理
- **合规**：GDPR、SOC2 等合规要求的技术实现

示例：
```
- ADR-004：采用 JWT + OAuth 2.0 做服务间认证
- ADR-005：引入 Redis 做热点数据缓存
- ADR-006：使用 Circuit Breaker 模式处理第三方依赖故障
```

### 3. 依赖关系

组件间耦合方式和外部依赖的选择：

- **服务间调用**：同步 vs 异步、RPC vs 消息队列
- **共享库**：哪些能力抽取为公共库
- **第三方服务**：为什么选择这个供应商
- **数据共享**：数据库共享 vs API 隔离

示例：
```
- ADR-007：订单服务与库存服务通过消息队列异步通信
- ADR-008：使用 Kafka 替代 RabbitMQ 作为事件总线
- ADR-009：支付对接选择 Stripe 而非自建
```

### 4. 接口

API 设计、协议选择和契约定义：

- **API 风格**：REST vs GraphQL vs gRPC
- **版本策略**：API 版本号方案、兼容性承诺
- **数据格式**：JSON Schema、Protobuf、Avro
- **事件契约**：消息格式、Schema Registry
- **错误处理**：统一错误码、错误响应格式

示例：
```
- ADR-010：对外 API 采用 RESTful 风格，内部服务间使用 gRPC
- ADR-011：API 版本通过 URL 路径区分（/v1/、/v2/）
- ADR-012：错误响应统一为 { code, message, details } 格式
```

### 5. 构造技术

开发过程中使用的工具、库和流程：

- **语言 / 运行时**：为什么选择这个技术栈
- **框架选择**：Web 框架、ORM、测试框架
- **数据库选型**：关系型 vs NoSQL、具体产品选择
- **基础设施**：容器编排、CI/CD 工具、监控方案
- **开发流程**：分支策略、发布流程、代码审查流程

示例：
```
- ADR-013：数据持久化选择 PostgreSQL 而非 MySQL
- ADR-014：采用 Docker Compose 做本地开发环境
- ADR-015：日志收集使用 Loki + Grafana
```

## ADR 文件组织

```
docs/adrs/
├── README.md                  # ADR 索引
├── 2024-01-10-monolith-to-microservices.md
├── 2024-01-15-jwt-auth-strategy.md
├── 2024-02-01-kafka-event-bus.md
└── ...
```

## ADR 索引

本文件是 agent 指南，不承载具体 ADR 索引。首次创建 ADR 时，同时创建 `docs/adrs/README.md`；后续创建新 ADR 后，在 `docs/adrs/README.md` 更新索引表。
