# 头脑风暴

人类与 coding agent 之间的创意协作协议。agent 在脑暴中的角色不是被动应答，而是主动的**方案生成者**——提供选项、对比优劣、暴露盲区。

## agent 的行为准则

在脑暴中，agent 必须做到以下三条：

### 1. 脑暴时必须给出选项

不要只给一个方案。进入脑暴或设计探索阶段后，每个问题至少给出 **2-3 个可行方向**，并说明各自的 trade-off。若任务已经明确到可以直接执行（如 tiny 修改、单一正确答案的 Bug 修复），无需强行脑暴，只需说明采用的路径。

```
差："我建议用 Redis 做缓存。"
好："缓存层有三个方向：
  A. Redis — 成熟、高性能，需要额外运维
  B. 应用内存缓存 — 零依赖，但重启丢失、多实例不一致
  C. 数据库查询缓存 — 不改架构，但提升有限

推荐 A，理由是..."
```

### 2. 主动追问需求

人类说"帮我做个登录"，不要直接开始设计。先追问：

- 用户量级？QPS 预期？
- 需要支持哪些登录方式？（密码 / 验证码 / OAuth / 生物识别）
- 安全等级？是否需要 MFA？
- 是否需要记住登录状态？多久过期？
- 兼容哪些终端？（Web / 小程序 / App）

**追问的目的不是拖延，而是避免基于错误假设做方案。**

### 3. 标注信心等级

对每个输出标注 agent 的信心程度：

- ✅ **确定**：有充分依据，可直接采纳
- 🤔 **有把握**：合理推断，建议验证
- ❓ **猜测**：缺乏信息，仅供参考，需要人类确认

示例：
```
- 🤔 你们是 toB 的 SaaS，我推断用户量在万级，所以单体 + 垂直分库就够。
  （如果实际是百万级 DAU 的 toC 产品，这个推断就是错的，请纠正我。）
```

## 脑暴流程

```
[人] 提出问题 / 目标
        │
[agent] ① 理解确认
        用自己的话复述问题和约束，确保理解正确
        │
[agent] ② 探索方案
        按以下维度系统性地生成方案：
        ├─ 简单方案：最小改动，最快交付
        ├─ 标准方案：业界成熟做法
        ├─ 激进方案：技术前沿、高收益高风险
        └─ 折中方案：平衡复杂度与收益
        │
[agent] ③ 评估对比
        每个方案标注：
        ├─ 复杂度（低 / 中 / 高）
        ├─ 风险（低 / 中 / 高）
        ├─ 实现成本（人天估算）
        ├─ 长期维护成本
        └─ 与现有架构的兼容性
        │
[人+agent] ④ 讨论 & 收敛
        双方讨论，淘汰不合适的方向，
        聚焦 1-2 个候选方案深入细化
        │
[agent] ⑤ 输出结论
        最后的脑暴结果输出为结构化文档
        （可以接入 design.md 的方案模板）
```

## 方案生成框架

agent 在生成方案时，系统性检查以下维度，确保没有遗漏：

### 维度清单

| 维度 | 检查项 |
| --- | --- |
| **功能** | 是否满足需求？有没有遗漏的边界条件？ |
| **架构** | 是否符合 `docs/architecture.md` 的整体设计？ |
| **原则** | 是否符合 `docs/agents/principles.md` 中的核心原则？ |
| **安全** | 有没有安全漏洞？权限、注入、泄漏、越权？ |
| **性能** | 瓶颈在哪？需要多少资源？能扛多大流量？ |
| **成本** | 开发成本多少？运维成本多少？第三方服务费用？ |
| **时间** | 多久能做出来？有没有可以分步交付的路径？ |
| **依赖** | 引入什么新依赖？依赖的稳定性如何？ |
| **团队** | 团队有没有相关经验？学习成本多高？ |
| **替代** | 如果这个方案行不通，Plan B 是什么？ |

> ⚠️ 如果方案越来越复杂、或者所有方案看起来都差不多，翻到
> `docs/agents/think-out-of-box.md` 重新审视问题本身。

### 反馈模板

agent 每次输出方案时，用以下格式组织：

```markdown
## 方案探索：<问题简述>

### 需求理解

<用自己的话复述问题，标注不清楚的地方>

### 方案 A：<名称>（推荐 / 备选）

- **思路**：<一句话说清楚>
- **复杂度**：低 / 中 / 高
- **实现成本**：约 X 人天
- **优点**：
  - ...
- **缺点**：
  - ...
- **风险**：
  - ...（缓解措施：...）
- **信心**：✅ / 🤔 / ❓

### 方案 B：<名称>

（同上）

### 方案 C：<名称>

（同上）

### 对比矩阵

| 维度 | 方案 A | 方案 B | 方案 C |
| --- | --- | --- | --- |
| 复杂度 | 低 | 中 | 高 |
| 交付时间 | 3 天 | 1 周 | 2 周 |
| 性能 | 满足 | 优良 | 卓越 |
| 扩展性 | 低 | 中 | 高 |
| 风险 | 低 | 中 | 高 |

### 推荐

推荐方案 A，因为...（简要理由）。
如果未来需求增长到 X，可以考虑迁移到方案 B。
```

## 常见脑暴场景

- **技术选型**：列出 2-4 个可行技术方向，追问规模、延迟、双向通信、运维成本等关键约束
- **数据建模**：先识别核心实体、状态机和生命周期，再追问数据量、保留周期、合规要求
- **重构策略**：优先评估测试覆盖、依赖范围和持续交付要求，再比较渐进式改造和重写方案

## 脑暴产出物

脑暴不默认写入仓库。只有在人类明确要求沉淀、方案已收敛且需要长期追踪时，agent 才在 `docs/brainstorm/` 目录下输出结构化笔记；首次写入时创建该目录。

```
docs/brainstorm/
└── YYYY-MM-DD-<主题>.md
```

笔记最小格式：

```markdown
# 脑暴记录：<主题>

- **日期**：YYYY-MM-DD
- **状态**：讨论中 / 已收敛 / 已转入设计

## 问题

<原始问题描述>

## 方案对比

| 方案 | 复杂度 | 风险 | 推荐度 |
| --- | --- | --- | --- |
| A | ... | ... | ... |
| B | ... | ... | ... |

## 待澄清

- [ ] <需要确认的假设>

## 下一步

- [ ] <下一步动作>
```

## agent 的脑暴边界

不是所有问题都适合脑暴。agent 应该判断：

**该脑暴**：
- 技术选型、架构决策、方案设计
- 多个可行方向，需要对比 trade-off
- 需求不明确，需要追问澄清

**不该脑暴**：
- 需求和执行路径都已经明确，说明采用路径后直接开始做
- 纯粹的 Bug 修复（只有一个正确答案）
- 拼写错误、格式调整等 tiny 任务

**不确定时，默认脑暴**——花 10 分钟讨论清楚，比花 10 小时在错误的方向上编码划算。
