# 跳出盒子思考

当标准方案感觉不对劲时，翻到这里。本页不是流程，是一组**反诘卡片**——agent 对着自己问一遍，逼自己跳出舒适区。

## 何时触发

以下任一信号出现时，强制翻到本页重想一遍：

- 🚩 **信号 1**：你给出的方案和上次、上上次几乎一样。"又是引入消息队列解耦"——真的需要吗？
- 🚩 **信号 2**：人类说"还有没有别的思路？""这个方案太保守了""有没有更大胆的想法？"
- 🚩 **信号 3**：方案越来越复杂，但问题本身从来没被质疑过。

## 反诘卡片

### 第一组：质疑前提

在动手解决之前，先想清楚这真的是问题。

> **这个问题值得解决吗？**
>
> 不解决会怎样？一星期后会怎样？一个月后会怎样？如果答案是"好像也不会怎样"——那就别做。

> **能不能不写代码？**
>
> 改配置能实现吗？改文档能实现吗？改流程能实现吗？跟人沟通能实现吗？
> 每一行不写的代码都是零 Bug、零维护成本。

> **问题是什么，而不是症状是什么？**
>
> "订单查询慢"是症状。"订单表扫了全表"是原因。"应该归档冷数据"还是"应该加异构索引"才是方案。有没有可能是别的根本原因？

### 第二组：挑战方案

> **能不能删代码，而不是加代码？**
>
> 删掉那个没人用的功能，问题就消失了。删掉那个过度抽象，维护成本就降了。每次改动前先问：我能删掉什么？

> **如果只能改一个文件，怎么做？**
>
> 不是调侃。约束自己在一个文件内解决问题，会逼出最简洁的方案。如果不行，两个文件。你很可能不需要动 8 个文件。

> **能不能用已有的东西拼出来？**
>
> 项目里有没有现成模块能做到 80%？引入新依赖的长期成本，往往比现在想象的更大。先穷尽现有资源。

### 第三组：拉伸尺度

> **如果流量涨 10 倍，这个方案还成立吗？反过来，如果只有 1/10 的流量呢？**
>
> 过设计是病。一个百人用的内部工具，不需要分布式事务。一个千万 DAU 的产品，SQLite 撑不住。答案取决于真实的量级，不是想象。

> **三年后回头看你今天的设计，会觉得骄傲还是尴尬？**
>
> 写出让自己三个月后骂街的代码，每个工程师都干过。努力不在"三个月后骂自己"的上限之下。

> **最差情况下，这个方案会以什么方式失败？**
>
> 把所有假设列出来：假设 Redis 不会挂、假设流量不会暴涨、假设上游 API 不 rate-limit。然后逐一问：如果这个假设不成立，会发生什么？最坏结果是什么？能不能接受？

## agent 的行为授权

以下指令是明确的——agent 有权做这些事，不需要逐次请求许可：

1. **你有权质疑需求本身**。"这个功能真的需要吗？不做的代价是什么？"——礼貌地问，但不允许不思考。

2. **你有权给"不写代码"的方案**。如果改文档、改流程、改配置能解决问题，那就是一个有效的方案，不比写代码低一等。

3. **你有权在每轮脑暴后翻到这里自检一遍**。默认方案是第一个出现在你脑海里的那个——它大概率是主流答案，也大概率不够好。

4. **你有权说"我不知道"**。如果问题超出你的知识边界——承认，反问人类，比假装有答案强一万倍。

## 使用方式

```
[脑暴] → [出方案] → [翻到这里自检一遍]
                         │
                    ┌────┴────┐
                    │ 通了？    │ 卡了？
                    ▼          ▼
                继续设计     [回脑暴重想]
                           [质疑前提]
                           [挑战方案]
                           [拉伸尺度]
```

**不是每次都用**。日常 tiny/small 任务不需要翻这里。只有 medium 以上、或者触发了三个信号之一时才用。

---

> 💡 提示：本页不该有一句废话。如果你读完没有任何一个问题让你停下来想了想，说明你还没跳出盒子。把这一页再读一遍。
