# 任务管理

人类和 coding agent 之间的任务协作协议。好的任务描述让 agent 一次做对，减少来回沟通。

## 任务描述

{{#if (eq tasks.style "结构化")}}

### 标准模板

```markdown
## 任务：<一句话简述>

- **类型**：feature / fix / refactor / chore
- **规模**：tiny / small / medium / large
- **背景**：为什么需要这个改动
- **目标**：改完之后达到什么效果
- **约束**：
  - 不能改动 XXX
  - 必须兼容 YYY
- **验收标准**：
{{#if (eq tasks.acFormat "checklist")}}
  - [ ] 功能 A 正常工作
  - [ ] 功能 B 正常降级
  - [ ] 现有测试全部通过
{{else}}
  - Given <前置条件>
  - When <触发动作>
  - Then <预期结果>
{{/if}}
```

{{else}}

### 轻量格式

```markdown
## 任务：<一句话描述>

<背景和目标，2-3 句话>

验收：
- 关键验收点 1
- 关键验收点 2
```

{{/if}}

## 规模定义

agent 根据任务规模自动判断是否需要创建隔离工作区（见 `docs/agents/vcs.md`）：

| 规模 | 描述 | 典型改动量 | 隔离策略 |
| --- | --- | --- | --- |
| **tiny** | 修拼写、改注释、调配置 | 1 文件，≤ 20 行 | 直接修改 |
| **small** | 小功能、单点修复 | 1-3 文件，≤ 100 行 | 按 vcs.md 策略 |
| **medium** | 完整功能、模块重构 | 3-10 文件 | 建议隔离 |
| **large** | 跨模块、架构变更 | 10+ 文件 | 必须隔离 |

## 执行策略

不是所有 coding agent 任务都应该交给 subagent。主 agent 负责理解目标、拆分边界、保护关键路径、整合结果和最终验收；subagent 只用于边界清晰、可并行、不会阻塞当前下一步的任务。

### 是否使用 subagent

| 规模 | 默认策略 | subagent 使用方式 |
| --- | --- | --- |
| **tiny** | 主 agent 直接做 | 不委派 |
| **small** | 主 agent 直接做 | 通常不委派；只有独立验证或搜索可并行时才委派 |
| **medium** | 主 agent 主导 | 可委派探索、验证、局部实现等不重叠子任务 |
| **large** | 主 agent 拆分并整合 | 可拆成多个互不重叠的子任务并行委派 |

委派前必须确认：

- 子任务目标、输入、输出和验收标准明确
- 文件 / 模块 ownership 清楚，多个 subagent 不写同一片区域
- 子任务不是当前关键路径上的阻塞任务
- subagent 知道不要回滚他人改动，要基于当前状态协作

### Reasoning effort

| Effort | 适用任务 |
| --- | --- |
| `low` | 拼写、格式、简单文档、小范围机械替换、明确 tiny 任务 |
| `medium` | 常规 Bug 修复、小功能、单模块改动、测试补充 |
| `high` | 跨模块重构、复杂故障定位、安全 / 数据一致性、需要明显取舍的设计 |
| `xhigh` | 架构决策、迁移方案、高风险生产问题、多方案深入比较 |

默认用能可靠完成任务的最低 effort。提高 effort 的理由应来自任务风险、耦合度或不确定性，而不是任务看起来重要。


## agent 接到任务后的工作流

1. **理解确认**：用自己的话复述任务目标，确认理解无误
2. **规模评估**：根据改动范围评估任务规模
3. **执行策略**：决定是否使用 subagent，并选择合适的 reasoning effort
4. **测试策略**：判断是否需要先写 RED 测试，以及用什么方式验证（详见 `docs/agents/testing.md`）
5. **脑暴探索**：必要时先脑暴方案选项（参考 `docs/agents/brainstorm.md`）
6. {{#if (ne vcs.isolation "none")}}**环境准备**：根据规模决定是否创建隔离工作区{{else}}**环境准备**：切换到对应分支开始工作{{/if}}
7. **方案设计**：必要时先出方案（参考 `docs/agents/design.md`）
8. **编码实现**：对照原则（`docs/agents/principles.md`）。有行为变化的改动遵循 Red / Green / Refactor（详见 `docs/agents/testing.md`）
9. **自检**：对照评审清单自查（`docs/agents/review.md`）
10. **交付**：提交代码，说明改动内容和验证方式
