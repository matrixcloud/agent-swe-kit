# 任务管理

人类和 coding agent 之间的任务协作协议。好的任务描述让 agent 一次做对，减少来回沟通。

## 任务描述

{{#if (eq tasks.style "结构化")}}

### 标准模板

```markdown
## 任务：<一句话简述>

- **类型**：feature / fix / refactor / chore
- **规模**：tiny / small / medium / large
- **背景**：为什么需要这个改动
- **目标**：改完之后达到什么效果
- **约束**：
  - 不能改动 XXX
  - 必须兼容 YYY
- **验收标准**：
{{#if (eq tasks.acFormat "checklist")}}
  - [ ] 功能 A 正常工作
  - [ ] 功能 B 正常降级
  - [ ] 现有测试全部通过
{{else}}
  - Given <前置条件>
  - When <触发动作>
  - Then <预期结果>
{{/if}}
```

{{else}}

### 轻量格式

```markdown
## 任务：<一句话描述>

<背景和目标，2-3 句话>

验收：
- 关键验收点 1
- 关键验收点 2
```

{{/if}}

## 规模定义

根据任务规模，agent 自动判断是否使用 Git Worktree 创建独立物理工作区，避免不同任务互相干扰。选择后，按照 `docs/agents/vcs.md` 中的命令操作。

| 规模 | 描述 | 典型改动量 | Git Worktree |
| --- | --- | --- | --- |
| **tiny** | 修拼写、改注释、调配置 | 1 文件，≤ 20 行 | 不需要 |
| **small** | 小功能、单点修复 | 1-3 文件，≤ 100 行 | 单文件不需要，跨模块建议创建 |
| **medium** | 完整功能、模块重构 | 3-10 文件 | 建议创建 |
| **large** | 跨模块、架构变更 | 10+ 文件 | 必须创建 |

## Subagent 执行策略

以下任务类型天然适合 subagent——边界清晰、输入输出明确、不涉及架构决策：

| 类型 | 典型场景 | 推荐 Effort |
| --- | --- | --- |
| **探索/调研** | 调研某个库的 API、分析现有代码模式、验证方案可行性 | `medium` |
| **独立模块实现** | large 任务拆分后互不重叠的子模块 | `high` |
| **测试编写** | 为已有接口补测试覆盖、编写边界条件测试 | `medium` |
| **独立 chore** | 依赖升级、批量重命名、配置迁移 | `low` |
| **Bug 定位（只读）** | 根据现象定位根因和涉及文件 | `medium` |
| **Review / 自检** | 代码评审、按 review.md 清单逐项检查 | `xhigh` |
| **文档生成** | 从代码生成 API 文档、变更日志、架构图更新 | `low` |

默认用能可靠完成任务的最低 effort。提高 effort 的理由应来自任务风险、耦合度或不确定性。


## agent 接到任务后的工作流

1. **理解确认**：用自己的话复述任务目标，确认理解无误
2. **规模评估**：根据改动范围评估任务规模
3. **执行策略**：决定是否使用 subagent，并选择合适的 reasoning effort
4. **测试策略**：判断是否需要先写 RED 测试，以及用什么方式验证（详见 `docs/agents/testing.md`）
5. **脑暴探索**：必要时先脑暴方案选项（参考 `docs/agents/brainstorm.md`）
6. {{#if (ne vcs.isolation "none")}}**环境准备**：根据规模决定是否创建 Git Worktree{{else}}**环境准备**：切换到对应分支开始工作{{/if}}
7. **方案设计**：必要时先出方案（参考 `docs/agents/design.md`）
8. **编码实现**：对照原则（`docs/agents/principles.md`）。有行为变化的改动遵循 Red / Green / Refactor（详见 `docs/agents/testing.md`）
9. **自检**：对照评审清单自查（`docs/agents/review.md`）
10. **交付**：按 `docs/agents/vcs.md` 的规范（分支命名、commit 格式、合并策略）提交代码，说明改动内容和验证方式
